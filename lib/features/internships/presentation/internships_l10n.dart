// Internship enum → localized label extensions live in `shared` (next to the
// [InternshipDetails] model) so the shared `JobDetailView` can use them too.
// Re-exported here for a convenient feature-local import.
export '../../../shared/models/internship_details_l10n.dart';
